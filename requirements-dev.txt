# VaultScope - development & test dependencies.
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.24.0
httpx==0.27.2          # FastAPI TestClient transport
ruff==0.8.4            # lint + format
