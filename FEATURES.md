# VaultScope — Feature Queue

Status of the full feature list. ✅ done & on `main` · 🟡 partial · ⬜ not started.

## 🔴 Critical path

| # | Feature | Status | Where |
|---|---|---|---|
| 1 | VID fingerprinting DB (Cisco / Juniper / strongSwan / Palo Alto / Fortinet …) | ✅ | `core/ike_parser/_vendor_ids.py`, sets `ike.vendor` |
| 2 | Aggressive Mode via ID payload (not message count) | ✅ | `core/ike_parser/ikev1.py` (P2-T2) |
| 3 | IKEv1 Quick Mode PFS (KE payload → PFS on) | ✅ | `core/ike_parser/ikev1.py` (P2-T3) |
| 4 | Full pipeline wiring: `/ingest` → parser → features → classifiers → rules → scoring → DB → `/sessions` | ✅ | `core/pipeline.py`, `api/main.py` |
| 5 | Mock data server (fixture JSON) for the frontend | ✅ | `data/mock/*.json` + pipeline FIXTURE MODE |

## 🟠 High impact

| # | Feature | Status | Notes |
|---|---|---|---|
| 6 | Retransmission timing fingerprint (Cisco ~10 s, strongSwan ~3 s) | ⬜ | secondary signal when VID absent |
| 7 | Downgrade attack detection (responder accepts the weakest of N proposals) | ⬜ | runtime threat, not static config |
| 8 | Packet-level evidence on anomalies (`evidence_pkts: [int]`) | 🟡 | field exists on `AnomalyEvent`, always `[]` — needs the ingestion layer to carry packet numbers |
| 9 | D3 peer graph: vendor labels + risk colouring | ✅ | `frontend/src/components/PeerGraph.tsx` |
| 10 | Session drilldown panel | ✅ | `frontend/src/components/SessionDrilldown.tsx` |

## 🟡 Strong talking points

| # | Feature | Status | Notes |
|---|---|---|---|
| 11 | Dead Peer Detection flag (DPD notify absent → zombie sessions) | ⬜ | extract from IKE_AUTH Notify |
| 12 | SA lifetime anomaly: observed rekey interval vs declared lifetime | ⬜ | per peer-pair timing |
| 13 | Certificate analysis on RSA auth (expiry, key length, sig alg) | ⬜ | CERT payload in IKE_AUTH; expired → CRITICAL |
| 14 | Transform brute-force anomaly (>N SA_INIT from one IP, varying proposals) | ✅ | `core/anomalies.py::detect_transform_bruteforce` |
| 15 | SPI replay detector (same SPI across concurrent sessions) | ✅ | `core/anomalies.py::detect_spi_collision` |
| 16 | IKE fragmentation flag + CVE-2016-1287 linkage on Cisco ASA | ✅ | `ike.fragmented_ike` + rule R14 (P2-T4) |
| 17 | WebSocket live session stream (new session in table < 2 s) | ✅ | `api/main.py` `/ws/live` + `frontend/src/app/live` |
| 18 | Executive PDF report (1–2 pages, plain English, top 3) | ✅ | `reporting/render.py` |

## 🟢 Demo polish

| # | Feature | Status | Notes |
|---|---|---|---|
| 19 | IKE message size pattern logging (tertiary fingerprint) | 🟡 | `core/classifiers/protocol.structural_features` computes it; not logged per session |
| 20 | IPv6 + IKEv1 hygiene flag (LOW finding) | ⬜ | one rule |
| 21 | NAT-T unexpected-peer flag (IKE on UDP 4500 where not expected) | ⬜ | anomaly |
| 22 | Historical diff view (two pcaps → added/removed peers, degraded scores) | ✅ | `/sessions/diff` + `frontend/src/app/compare` |
| 23 | Confusion matrix embedded in the technical report | ✅ | `reporting/render.py` reads `models/confusion_matrix.json` |
| 24 | Feature-importance chart in the dashboard | 🟡 | `models/feature_importance.png` generated by `core.classifiers.train`; dashboard tile TBD |
| 25 | CEF export validation against a CEF parser | ✅ | `reporting/export.py` + `tests/reporting/test_export.py` |
| 26 | WhatsApp-proxy disclosure section in the technical report | ✅ | testbed docs + report template |
| 27 | `docker compose up` one-command stack | ✅ | `docker-compose.yml` + per-service Dockerfiles + nginx |

## ❌ Skipped (by decision)

1D-CNN traffic classifier · real-time remote agent · graph database · any LLM integration.

---

## Pipeline stages

| Stage | Owner | Status |
|---|---|---|
| 0 Testbed / dataset generator | Block A/B | code ✅ · **dataset not yet generated** |
| 1 Ingestion engine (`core/ingestion`) | Block A | ✅ |
| 2 IKE parser (`core/ike_parser`) — IKEv1 + IKEv2 | Block A | ✅ (P2-T1..T4) |
| 3 Flow feature extractor (`core/flow`) | Block A | ✅ |
| 4a Protocol/crypto classifier (`core/classifiers`) | Block A | ✅ (synthetic bootstrap model) |
| 4b Traffic-type classifier (`core/classifiers`) | Block A | ✅ (synthetic bootstrap, macro-F1 ≈ 0.79) |
| 4c Security rule engine (`core/rules`) | Block B | ✅ (15 rules) |
| 5 Scoring + reports (`reporting`) | Block B | ✅ (exec PDF, tech HTML, JSON, CEF) |
| 6 Dashboard (`frontend`) | Block B | ✅ (Next.js: overview, sessions, graph, live, compare, export) |

**Bootstrap models** (`models/*.pkl`) are trained on synthetic per-class flows.
Re-run `python -m core.classifiers.train` once the Stage 0 pcap dataset exists.
