"""FastAPI backend + WebSocket layer (owner: P3).

Placeholder package created during environment setup. P3 owns the
implementation (upload/analyze endpoints + live-capture WebSocket) against the
API contract in the product spec, Section 11.
"""
